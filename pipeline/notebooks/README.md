# Notebooks

Exploratory notebooks for texture processing research.

## Conventions

- One notebook per exploration topic
- Name notebooks descriptively: `ring_transparency_analysis.ipynb`, not `notebook1.ipynb`
- Launch Jupyter from the `pipeline/` directory: `uv run jupyter lab`
- Notebooks are for exploration only — production logic belongs in `src/pipeline/`
